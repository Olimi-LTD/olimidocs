---
title: 'Generate Audio'
openapi: 'POST /audio/generate'
---

Generates spoken audio from text using a specified voice model. This endpoint accepts a JSON payload with a `voice_id` and a `sentence` that you wish to convert into speech.
